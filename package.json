{
  "name": "elitea",
  "private": true,
  "version": "0.0.0",
  "description": "Playwright tests for EPAM Insights navigation",
  "devDependencies": {
    "@playwright/test": "^1.54.0",
    "typescript": "^5.5.0"
  },
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  }
}
